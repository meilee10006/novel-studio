services:
  novel-core:
    image: ghcr.io/meilee10006/novel-studio:latest
    build:
      context: .
      args:
        GOPROXY: ${GOPROXY:-https://goproxy.cn,direct}
    volumes:
      - ./workspace:/workspace
    stdin_open: true
    tty: true
